# blogSys

一个多人博客平台。内容由人创作，平台只负责让人找到它 —— 这个立场决定了很多取舍，比如平台不提供写作 Agent，AI 助手只做检索。

## Language

### 读者与身份

**Viewer**（浏览者）：
正在读这份内容的人。可能已登录，也可能未登录 —— 「未登录」是一种正常的 viewer，不是缺失状态。
_Avoid_: 用户、当前用户、principal、caller、请求方

**Owner**（所有者）：
内容的作者本人，对其内容有写权限。
_Avoid_: 作者（"作者"指展示意义上的署名，Owner 指权限意义上的归属）、creator

**Admin**（管理员）：
一种角色。对可见性规则全域豁免 —— 看得见平台上的一切内容。
_Avoid_: 超管、moderator、superuser

### 内容的可见状态

**Visibility**（可见性）：
「这份内容此刻对这个 viewer 是否可读」这一个判断。只回答读的问题。
_Avoid_: 权限、access control、authorization（这些词同时涵盖写操作，会把两件事混起来）

**Disclosure**（披露）：
单份内容对这个 viewer 而言是否存在。与 Visibility 是同一个判断的两种用法：用在单份内容上得到「存在 / 不存在」，用在列表上得到过滤。
关键不变量：**不可见与不存在必须不可区分** —— 对不可见的单份内容一律作「不存在」处理，否则等于确认了它的存在。
_Avoid_: existence、可访问性

**Banned**（封禁）：
用户的一种状态。被封禁者的内容对非管理员不可见，其本人也不能登录。
_Avoid_: disabled、blocked、suspended、封号

**Draft**（草稿）：
文章的一种状态。只对 Owner 与 Admin 可见。
_Avoid_: unpublished、private、未发布

**Published**（已发布）：
文章的一种状态。对所有 viewer 可读 —— 除非其 Owner 被封禁。
_Avoid_: live、active、public（"public" 会掩盖「作者被封禁时它并不可读」这层含义）

### 需要留意的词义冲突

**articleCount**：
这个字段名目前在两个地方含义不同：公开主页上指「已发布文章数」，管理后台用户列表里指「全部文章数（含草稿）」。**同一字段名不得有两种含义** —— 调用前必须确认是哪一种，长期应拆成两个名字。
_Avoid_: 笼统地用"文章数"

**封禁用户的昵称/用户名**：
被封禁者的署名信息仍在 RSS 与部分响应里出现。「内容不可见」不等于「这个人不存在」—— 涉及署名时要明确是有意还是漏判。
